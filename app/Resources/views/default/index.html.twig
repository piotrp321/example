{% extends 'base.html.twig' %}
{% block body %}
    <div class="jumbotron">
        <h1 class="display-4">Hello, anymous!</h1>

        <hr class="my-4">
        <a class="btn btn-primary btn-lg" href="{{ path('fos_user_security_login') }}" role="button">
            {{ 'layout.login'|trans({}, 'FOSUserBundle') }}
        </a>
        <a class="btn btn-primary btn-lg" href="{{ path('fos_user_registration_register') }}" role="button">
            {{ 'layout.register'|trans({}, 'FOSUserBundle') }}
        </a>
    </div>
{% endblock %}
